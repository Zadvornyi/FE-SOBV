import { SobvLimitStringPipe } from './sobv-limit-string.pipe';

describe('SobvLimitStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SobvLimitStringPipe();
    expect(pipe).toBeTruthy();
  });
});
