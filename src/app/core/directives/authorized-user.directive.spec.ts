import { AuthorizedUserDirective } from './authorized-user.directive';

describe('AuthorizedUserDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthorizedUserDirective();
    expect(directive).toBeTruthy();
  });
});
